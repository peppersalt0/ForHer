# Assets Folder

Put your images and GIFs here!

Supported formats:
- PNG images (.png)
- JPEG images (.jpg, .jpeg)
- GIF animations (.gif)

Then reference them in the code like:
```python
img_path = os.path.join(os.path.dirname(__file__), 'assets', 'your_image.png')
```
